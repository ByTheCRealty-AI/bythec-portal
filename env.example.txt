# By the C — Sistema (Onda 2) · variáveis de ambiente (TEMPLATE).
#
# >>> RENOMEIE este arquivo para `.env.example` e copie para `.env.local` no local. <<<
# (A política de segurança do repo BA bloqueia escrita direta em `.env*`, por isso
#  o template chega como `env.example.txt`. Ao virar repo próprio do cliente, vira `.env.example`.)
#
# NUNCA versione `.env.local` nem valores reais (já está no .gitignore).
#
# Pré-Auth: o painel é INTERNO e lê/escreve via service_role NO SERVIDOR.
# A service_role NUNCA vai pro browser (sem prefixo NEXT_PUBLIC_). Trocar por
# anon key + Supabase Auth + policies quando o login entrar.
#
# Na Vercel, configure as 3 abaixo em Project Settings · Environment Variables.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
