# By the C — Sistema (Onda 2) · variáveis de ambiente (TEMPLATE).
#
# >>> RENOMEIE este arquivo para `.env.example` e copie para `.env.local` no local. <<<
# (A política de segurança do repo BA bloqueia escrita direta em `.env*`, por isso
#  o template chega como `env.example.txt`. Ao virar repo próprio do cliente, vira `.env.example`.)
#
# NUNCA versione `.env.local` nem valores reais (já está no .gitignore).
#
# AUTH LIGADO (Onda 2, Fase Auth):
#  - O painel lê/escreve via SESSÃO DO USUÁRIO logado (anon key + cookies + RLS).
#  - A service_role fica SÓ no servidor, usada apenas pela action de convite/
#    deleção de usuário (auth.admin). NUNCA vai pro browser (sem NEXT_PUBLIC_).
#  - NEXT_PUBLIC_SITE_URL é a URL pública (usada no redirect do link de convite).
#
# Na Vercel, configure todas abaixo em Project Settings · Environment Variables.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# URL pública do app (ex.: https://bythec-portal.vercel.app). Sem barra no fim.
# Usada como redirectTo do convite (inviteUserByEmail). Se ausente, cai num default.
NEXT_PUBLIC_SITE_URL=
